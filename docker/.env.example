# === 构建配置 ===

# Caddy 版本号，留空则自动查询最新版本（需要容器能访问外网）
# 建议显式指定版本号以确保构建可复现，例如: CADDY_VERSION=2.9.1
CADDY_VERSION=
# 目标架构：x86_64 | aarch64 | all
TARGET_ARCH=all
# 目标发行版：distro:version,... | all
TARGET_DISTRO=all
# .repo 模板基础 URL
BASE_URL=https://rpms.example.com

# === 签名配置 ===

# GPG 密钥 ID（必填）
GPG_KEY_ID=

# === 服务配置 ===

# 服务域名（用于 Caddy 自动 HTTPS）
DOMAIN_NAME=rpms.example.com

# === 定时检查配置 ===

# 检查周期：Nd（天）、Nh（小时）
CHECK_INTERVAL=10d
# GitHub API 令牌（可选，避免匿名限流）
GITHUB_TOKEN=

# === 管理 API 配置（可选）===

# API 认证令牌
API_TOKEN=
# 管理 API 监听端口
MANAGER_PORT=8080

# === Docker Socket 权限 ===

# 宿主机 docker 组的 GID，用于 scheduler 和 repo-manager 访问 Docker socket
# 通过 stat -c '%g' /var/run/docker.sock 获取，默认 999
DOCKER_GID=
